/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 (via @tailwindcss/vite) automatically configures
 * all required PostCSS plugins.
 *
 * You do NOT need to add:
 * - tailwindcss
 * - autoprefixer
 *
 * This file is only used when additional PostCSS plugins are required.
 *
 * Example:
 *
 * import postcssNested from 'postcss-nested'
 *
 * export default {
 *   plugins: [postcssNested()],
 * }
 *
 * If no extra plugins are needed, this file can remain empty.
 */

export default {}
